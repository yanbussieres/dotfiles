
# PATH="/opt/homebrew/bin:opt/homebrew/opt/ruby/bin:$PATH:/Users/y/flutter/bin"
# export PATH="/opt/homebrew/opt/ruby/bin:$PATH"
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completio:q

eval "$(zoxide init zsh)"
eval "$(starship init zsh)"

alias ls='lsd'
alias l='ls -1'
alias ll='ls -l'
alias lg='ls -l -g'
alias la='ls -a'
alias lla='ls -la'
alias lt='ls --tree'


source /Users/y/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
# - $ZSH_CUSTOM/aliases.zsh
# - $ZSH_CUSTOM/macos.zsh
# alias zshconfig="mate ~/.zshrc"jjj
# alias ohmyzsh="mate ~/.oh-my-zsh"
#
# export ZSH="$HOME/.oh-my-zsh"
#
# plugins=(git brew flutter z eza gcloud colored-man-pages colorize npm )
#
# fpath+=${ZSH_CUSTOM:-${ZSH:-~/.oh-my-zsh}/custom}/plugins/zsh-completions/src
#
# source $ZSH/oh-my-zsh.sh
#



if [ -f '/Users/y/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/y/google-cloud-sdk/path.zsh.inc'; fi

if [ -f '/Users/y/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/y/google-cloud-sdk/completion.zsh.inc'; fi

function on {
    local suffix=$1
    local filename="/Users/y/vaults/work/$(date +%s)-${suffix}.md"
    nvim $filename +w
}
# plugins=(git brew flutter z eza gcloud colored-man-pages colorize npm )

. "$HOME/.atuin/bin/env"

eval "$(atuin init zsh)"

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/y/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/y/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/y/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/y/google-cloud-sdk/completion.zsh.inc'; fi
